# AI Image Sorter

AI Image Sorter is a local-first desktop application for organizing and reviewing AI-generated images. It is built for image libraries produced by ComfyUI and compatible generators, with generation metadata kept alongside the images in a local SQLite database.

## Features

- Discover and import ComfyUI output folders, or select a local folder manually
- Parse PNG, JPEG, and WebP generation metadata from ComfyUI, A1111, and related formats
- Group images by folder, model, prompt similarity, and independent prompt
- Match ComfyUI workflow topology and retain generation recipes
- Review images with swipe scoring or pairwise arena comparisons
- Hide, label, archive, move, merge, split, and undo image actions
- Incrementally rescan registered folders in the background
- Export image data as CSV/JSON and export redacted diagnostics
- Store application data locally; telemetry is disabled unless enabled in Settings

## Status

`v0.1.0` is the first public preview. The core workflow is usable, but compatibility with unusual metadata formats and large libraries is still being expanded. Windows is the primary supported platform for this release.

## Installation

Download the Windows installer from the [Releases](https://github.com/RePinkert/AI-Image-Sorter/releases) page. The NSIS installer and MSI package are generated by the Tauri release build.

The application does not upload or modify source images during indexing. Image files remain in their original folders unless an explicit archive, move, or recycle-bin action is requested.

## Usage

1. Launch the application.
2. Choose a detected ComfyUI output folder or select a local folder.
3. Let the initial scan finish. Registered folders are checked incrementally while the application is open.
4. Select a grouping level, then review images using swipe or arena mode.

The local database is stored in the platform-specific Tauri application data directory. It contains indexes, scores, labels, recipes, and optional local telemetry, but not copied image files.

## Development

### Requirements

- Node.js 20 or newer
- Rust 1.77.2 or newer
- Tauri 2 prerequisites for the target platform

### Commands

```bash
npm install
npm run dev          # Browser development shell
npm run tauri:dev   # Desktop development application
npm run build
npm run lint
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml
```

Some integration tests use a developer-local ComfyUI image directory and are not portable to a clean checkout. The portable unit tests do not require that directory.

## Privacy

The application is local-first. Optional telemetry records coarse application events for debugging and product improvement. It does not intentionally record prompts, image pixels, source paths, or other image contents. Telemetry can be disabled in Settings, and redacted diagnostics can be exported from the same panel.

## Contributing

Bug reports and pull requests are welcome. Please include the operating system, application version, reproduction steps, and a redacted diagnostics export when reporting a problem involving local data or metadata parsing.

## License

AI Image Sorter is licensed under the [MIT License](LICENSE).
